server:
  port: 8082
  shutdown: graceful

# SpringBoot config
spring:
  application:
    name: private-aider-service
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://${DB_HOST:localhost}:${DB_PORT:3306}/${DB_NAME:vikadata}?characterEncoding=utf8&autoReconnect=true&useSSL=true
    username: ${DB_USER:root}
    password: ${DB_PASSWORD:qwe123456}
    hikari:
      minimum-idle: 5
      maximum-pool-size: 15
      auto-commit: true
      idle-timeout: 30000
      pool-name: DatebookHikariCP
      max-lifetime: 1800000
      connection-timeout: 30000
      connection-test-query: SELECT 1
