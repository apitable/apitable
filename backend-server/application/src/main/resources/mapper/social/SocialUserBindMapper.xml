<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.vikadata.api.modular.social.mapper.SocialUserBindMapper">
    <select id="selectUserIdByUnionId" resultType="java.lang.Long">
        SELECT user_id
        FROM vika_social_user_bind
        WHERE union_id = #{unionId}
        LIMIT 1
    </select>

    <select id="selectUnionIdByUserId" resultType="java.lang.String">
        SELECT union_id
        FROM vika_social_user_bind
        WHERE user_id = #{userId}
    </select>

    <select id="selectByUnionIds" resultType="com.vikadata.entity.SocialUserBindEntity">
        SELECT *
        FROM vika_social_user_bind
        WHERE union_id IN
        <foreach item="item" index="index" collection="unionIds" open="(" separator="," close=")">
            #{item}
        </foreach>
    </select>

    <delete id="deleteByUnionIds">
        DELETE
        FROM vika_social_user_bind
        WHERE union_id IN
        <foreach item="item" index="index" collection="unionIds" open="(" separator=","
                 close=")">
            #{item}
        </foreach>
    </delete>

    <delete id="deleteByUserId">
        DELETE
        FROM vika_social_user_bind
        WHERE user_id = #{userId}
    </delete>

    <select id="selectOpenIdByTenantIdAndUserId" resultType="java.lang.String">
        SELECT vstu.open_id
        FROM vika_social_user_bind vsub
        JOIN vika_social_tenant_user vstu ON vstu.union_id = vsub.union_id
        AND vstu.tenant_id = #{tenantId} AND vstu.app_id = #{appId}
        WHERE vsub.user_id = #{userId}
    </select>
</mapper>
