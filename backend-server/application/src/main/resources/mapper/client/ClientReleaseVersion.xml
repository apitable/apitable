<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.vikadata.api.client.mapper.ClientReleaseVersionMapper">

    <select id="selectClientEntryDetailByVersion" resultType="com.vikadata.api.client.dto.ClientEntryDetailDto">
        select id, html_content, version, publish_user, `description`
        from ${tablePrefix}client_release_version
        where version = #{version}
        order by id desc
        limit 0,1
    </select>

    <select id="selectClientLatestVersionByOffset" resultType="java.lang.String">
        select version from ${tablePrefix}client_release_version order by id desc limit #{offset}, 1
    </select>

    <select id="selectClientLatestVersion" resultType="java.lang.String">
        select version from ${tablePrefix}client_release_version order by id desc limit 1
    </select>

    <select id="selectTotalCount" resultType="java.lang.Long">
        select count(1) from ${tablePrefix}client_release_version
    </select>
</mapper>
