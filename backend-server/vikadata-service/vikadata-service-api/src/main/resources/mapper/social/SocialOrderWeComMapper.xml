<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.vikadata.api.modular.social.mapper.SocialOrderWeComMapper">
    <select id="selectByOrderId" resultType="com.vikadata.entity.SocialOrderWecomEntity">
        SELECT *
        FROM vika_social_order_wecom
        WHERE order_id = #{orderId}
        AND is_deleted = 0
    </select>
    <select id="selectFirstPaidOrder" resultType="com.vikadata.entity.SocialOrderWecomEntity">
        SELECT *
        FROM vika_social_order_wecom
        WHERE suite_id = #{suiteId}
        AND paid_corp_id = #{paidCorpId}
        AND order_status = 1
        AND is_deleted = 0
        ORDER BY paid_time ASC
        LIMIT 1
    </select>
    <select id="selectLastPaidOrder" resultType="com.vikadata.entity.SocialOrderWecomEntity">
        SELECT *
        FROM vika_social_order_wecom
        WHERE suite_id = #{suiteId}
        AND paid_corp_id = #{paidCorpId}
        AND order_status = 1
        AND is_deleted = 0
        ORDER BY paid_time DESC
        LIMIT 1
    </select>
</mapper>
