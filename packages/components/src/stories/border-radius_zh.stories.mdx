import { StoryType } from './constants';
import { Meta } from '@storybook/addon-docs';

<Meta title={`${StoryType.Design}/Border Radius 圆角`} />

# Border Radius 圆角

默认使用 `4px` 圆角。

```css
border-raduis: 4px;
```