import { PageTurn } from 'pc/components/expand_record/page_turn';

export const WidgetPageTurn = () => {
  return <PageTurn
    preButtonTip={''}
    nextButtonTip={''}
    onClickPre={() => { }}
    onClickNext={() => { }}
    disablePre={false}
    disableNext={false}
  />;
};